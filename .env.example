# URL of the HealthTrack backend API.
# For local development (backend running on your machine):
REACT_APP_API_URL=http://localhost:5000/api

# For production, once the backend is deployed on Render, replace with something like:
# REACT_APP_API_URL=https://healthtrack-backend.onrender.com/api
